// 2020-02-22 19:42:36

//Split direction into x and y values
if(path_index != -1)
{
    var Dir = 180+point_direction(x,y,path_get_x(path_index,path_position-0.05),path_get_y(path_index,path_position-0.05));
    HSpeed = cos(degtorad(Dir))*WalkSpeed;
    VSpeed = -sin(degtorad(Dir))*WalkSpeed;
    //path_get_speed(path_index,path_position)
}